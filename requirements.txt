Flask>=3.0,<4.0
python-dotenv>=1.0
psycopg2-binary>=2.9      # Postgres (Vercel) — unused locally if no POSTGRES_URL
gunicorn>=21.2  ; sys_platform != "win32"
waitress>=3.0  ; sys_platform == "win32"
